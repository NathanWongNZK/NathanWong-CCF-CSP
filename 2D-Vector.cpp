#include <iostream>
#include <vector>
using namespace std;

int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    vector< vector<string> > test_2d_string;
    vector<string> string_to_push;
    string_to_push.push_back("A");
    test_2d_string.push_back(string_to_push);
}